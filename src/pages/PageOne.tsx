import { ContentContainer, SideBar } from 'src/components';

const PageOne = () => {
  return (
    <div className="page">
      <SideBar />
      <ContentContainer />
    </div>
  );
};

export { PageOne };
